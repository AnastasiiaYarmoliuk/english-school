# EngSchool LMS — Інформаційна система онлайн-школи

Повноцінна інформаційна система (Learning Management System), розроблена для автоматизації навчального процесу, фінансового моніторингу та управління користувачами в школі англійської мови.

## 🚀 Технологічний стек

Проєкт побудований на базі сучасного стеку **MERN** (з використанням Docker):

*   **Frontend:** [React](https://reactjs.org/) (збірка через **Vite**) — адаптивний та динамічний інтерфейс.
*   **Backend:** [Node.js](https://nodejs.org/) + [Express.js](https://expressjs.com/) — розробка RESTful API.
*   **Database:** [MongoDB](https://www.mongodb.com/) — нереляційна документоорієнтована база даних.
*   **Containerization:** [Docker](https://www.docker.com/) & [Docker Compose](https://docs.docker.com/compose/) — повна ізоляція та розгортання однією командою.
*   **Security:** [Bcrypt.js](https://www.npmjs.com/package/bcryptjs) — криптографічне хешування паролів.
*   **Styling & Icons:** [CSS Modules](https://github.com/css-modules/css-modules) та [Lucide React](https://lucide.dev/).

---

## 🛠 Як запустити проєкт

Для запуску системи **не потрібно** встановлювати Node.js, MongoDB чи інше ПЗ локально. Весь процес автоматизований через Docker.

### 1. Попередні вимоги
*   Встановлений [Docker Desktop](https://www.docker.com/products/docker-desktop/).
*   Встановлений інструмент [Git](https://git-scm.com/downloads).

### 2. Клонування та запуск
Відкрийте термінал та виконайте наступні команди:

```bash
# 1. Клонуйте репозиторій
git clone https://github.com/AnastasiiaYarmoliuk/english-school

# 2. Перейдіть у директорію проєкту
cd english-school

# 3. Запустіть систему (збірка та старт усіх контейнерів)
docker-compose up --build
```
Після появи повідомлення ✅ ALL SYSTEMS GO: Database fully seeded! у логах бекенду, система буде доступна в браузері.

---

## 🔌 Використані порти

*   **Клієнтська частина (React):** [http://localhost:3000](http://localhost:3000)
*   **Серверна частина (API):** [http://localhost:5000/api](http://localhost:5000/api)
*   **База даних (MongoDB):** `localhost:27017` (доступ через MongoDB Compass)

---

## 📊 Міграції бази даних

У проєкті реалізовано механізм **автоматичних міграцій (Database Seeding)**:

*   **Автоматизація:** Скрипт `backend/migrations/seed.js` запускається автоматично при кожному старті Docker-контейнера бекенду.
*   **Дані:** База самостійно очищується та наповнюється актуальним каталогом курсів, уроками, активними завданнями та тестовими обліковивими записами для всіх ролей (Адмін, Вчитель, Студент, Батько).
*   **Скидання:** Щоб виконати міграції заново на абсолютно чисту базу, використовуйте команду `docker-compose down -v`.

---

## 👥 Тестові дані для входу (Credentials)

Система підтримує 4 типи ролей з різним рівнем доступу для перевірки функціоналу:

| Роль | Email | Пароль | Можливості |
| :--- | :--- | :--- | :--- |
| **Студент** | `student@test.com` | `password123` | Навчальні модулі, прогрес-бар, здача ДЗ (завантаження файлів). |
| **Батько** | `parent@test.com` | `parent123` | Батьківський контроль: перегляд прогресу дитини та історії оплат. |
| **Вчитель** | `teacher@school.com` | `teacher123` | Перевірка ДЗ, виставлення балів, перегляд реєстру учнів. |
| **Адмін** | `admin@school.com` | `admin123` | **Код: ADM-777**. Повна аналітика, керування курсами та юзерами. |

> **🔐 Секретний вхід для Адміна:** На сторінці логіну натисніть 3 рази на заголовок **"З поверненням!"**, щоб з'явилося приховане поле для введення коду доступу `ADM-777`.

---

## 💻 Основні команди Docker

*   `docker-compose up` — швидкий запуск вже зібраної системи.
*   `docker-compose stop` — тимчасова зупинка роботи контейнерів.
*   `docker-compose down -v` — повне видалення контейнерів разом із даними та томами БД.
*   `docker-compose logs -f english_backend` — перегляд логів сервера в реальному часі для відладки.

---

## 📁 Структура проєкту

*   **/backend** — Серверна логіка (Node.js/Express):
    *   `/models` — Схеми даних для MongoDB (Mongoose).
    *   `/routes` — Розподілені маршрути API за функціональними модулями.
    *   `/migrations` — Логіка автоматичного наповнення бази даних.
    *   `/uploads` — Сховище для завантажених файлів (синхронізований Volume).
*   **/frontend** — Клієнтська частина (React/Vite):
    *   `/src/pages` — Окремі модулі для кожної сторінки та ролі.
    *   `/src/components` — Перевикористовувані елементи інтерфейсу та стилі.
*   **docker-compose.yml** — Конфігурація оркестрації всієї інфраструктури проєкту.

---
**Проєкт розроблено в рамках курсової роботи з дисципліни "Проектування та розробка ІС", 2024.**
